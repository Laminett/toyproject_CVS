{{>layout/header}}
<div class="content" id="context">
    <div class="container-fluid">
        <div class="row">
            <input type="hidden" id="loginUser" value="{{loginUser}}">
            <div class="col-md-12">
                <div class="card">
                    <div class="card-header card-header-primary">
                        <div style="float: left">
                            <h4 class="card-title">{{#messages}}title.settle{{/messages}}</h4>
                        </div>
                        <div class="form-row" style="float: right">
                            <div class="col form-row">
                                <input type="text" class="form-control monthpicker mp-monthpicker" placeholder="DATE" id="search_date">
                            </div>
                            <div class="col has-white">
                                <input type="text" class="form-control" id="search_username" placeholder="USER ID" maxlength="25">
                            </div>
                            <button type="button" class="btn btn-white btn-round btn-just-icon" id="btn_search">
                                <i class="material-icons">search</i>
                            </button>
                        </div>
                    </div>
                    <div class="card-body">
                        <table class="table">
                            <thead>
                            <tr class="text-center">
                                <th rowspan="2">#</th>
                                <th rowspan="2">{{#messages}}user.name{{/messages}}</th>
                                <th rowspan="2">date</th>
                                <th colspan="2">{{#messages}}approval{{/messages}}</th>
                                <th colspan="2">{{#messages}}cancel{{/messages}}</th>
                                <th colspan="2>">{{#messages}}total{{/messages}}</th>
                                <th rowspan="2">created date</th>
                                <th rowspan="2">{{#messages}}settle.date{{/messages}}</th>
                                <th rowspan="2">{{#messages}}state{{/messages}}</th>
                            </tr>
                            <tr class="text-center">
                                <th>{{#messages}}count{{/messages}}</th>
                                <th>{{#messages}}amount{{/messages}}</th>
                                <th>{{#messages}}count{{/messages}}</th>
                                <th>{{#messages}}amount{{/messages}}</th>
                                <th>{{#messages}}count{{/messages}}</th>
                                <th>{{#messages}}amount{{/messages}}</th>
                            </tr>
                            </thead>
                            <tbody>
                            {{#settle}}
                                <tr id="{{id}}" class="text-center">
                                    <td>{{id}}</td>
                                    <td class="text-left">{{username}}</td>
                                    <td>{{date}}</td>
                                    <td>{{approvalCount}}</td>
                                    <td>{{approvalAmount}}</td>
                                    <td>{{cancelCount}}</td>
                                    <td>{{cancelAmount}}</td>
                                    <td>{{totalCount}}</td>
                                    <td>{{totalAmount}}</td>
                                    <td>{{createdDate}}</td>
                                    {{#isApproved}}
                                     <td>{{modifiedDate}}</td>
                                     <td>Approved by {{adminId}}</td>
                                    {{/isApproved}}
                                    {{^isApproved}}
                                        {{#status}}
                                         <td>{{modifiedDate}}</td>
                                         <td> Denied by {{adminId}}</td>
                                        {{/status}}
                                        {{^status}}
                                         <td></td>
                                        <td class="td-actions text-center">
                                            <button type="button" class="btn btn-info" name="btn_approve" value="Y">
                                                <i class="material-icons">done</i>
                                            </button>&nbsp;
                                            <button type="button" class="btn btn-danger" name="btn_deny" value="N">
                                                <i class="material-icons">clear</i>
                                            </button>
                                        </td>
                                        {{/status}}
                                    {{/isApproved}}
                                </tr>
                            {{/settle}}
                            {{^settle}}
                                <tr>
                                    <td>{{#messages}}search.no.data{{/messages}}</td>
                                </tr>
                            {{/settle}}
                            </tbody>
                        </table>
                    </div>
                </div>
                <div>
                    <nav aria-label="Page navigation">
                        <ul class="pagination justify-content-center">
                            <li class="page-item disabled">
                                <a class="page-link" id="previous">Previous</a>
                            </li>
                            {{#page}}
                                <li class="page-item">
                                    <a class="page-link" id="page_{{{.}}}" value="{{{.}}}">{{{.}}}</a>
                                </li>
                            {{/page}}
                            <li class="page-item disabled">
                                <a class="page-link" id="next">Next</a>
                            </li>
                        </ul>
                    </nav>
                </div>
            </div>
        </div>
    </div>
</div>
<script>
    messages;
    messages.hello2 = '안녕?';

</script>
<link rel="stylesheet" href="/datepicker/bootstrap-monthpicker.css">
<script type="text/javascript" src="/datepicker/bootstrap-monthpicker.js"></script>
<script type="text/javascript" src="/js/settle.js"></script>
{{>layout/footer}}